@model IEnumerable<BYOD_Server.Models.Orders>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.promocod.promocodes_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.order_time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total_bill)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.promocod.promocodes_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.order_time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total_bill)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.order_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.order_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.order_id })
        </td>
    </tr>
}

</table>
